# Plant Your Flag Domain Badge

## Overview

For this assignment, I prepared my portfolio for public launch by
connecting it to a cleaner Vercel domain, adding privacy friendly
analytics, and checking the metadata used when the portfolio is shared
online.

The portfolio is currently live over HTTPS using the new Vercel URL.

## What I completed

### Domain and deployment

I updated the portfolio deployment so the live URL is more professional
and includes my name instead of the automatically generated Vercel
project URL.

The final live URL is:

**\[PASTE YOUR NEW VERCEL URL HERE\]**

The portfolio was also checked after deployment to make sure it loads
correctly from the final address.

### Analytics

I added GoatCounter analytics to the Next.js portfolio layout.

The analytics script was added to `app/layout.tsx` and connected to my
GoatCounter site. After deployment, I opened the live portfolio and
confirmed that the visit was registered in the GoatCounter dashboard.

![GoatCounter analytics showing a registered portfolio
visit](./goat-counter.png)

### Share preview and metadata

I verified the portfolio's Open Graph metadata using a metadata
inspection tool.

The preview correctly displays my portfolio title, description, and
preview image. The metadata inspector reported no critical errors.

![Open Graph preview image](./OG-image.png)

![Open Graph metadata verification](./Open%20Graph%20Verification.png)

The portfolio also includes:

-   A favicon
-   A page title
-   An Open Graph title
-   An Open Graph description
-   An Open Graph image
-   An Open Graph URL
-   A Twitter large image card

I also checked the live portfolio in a normal browser to confirm that
the favicon, title, and layout appear correctly.

## Graduate badge

The graduate verification badge is not included yet because my FlyRank
Backend AI Engineering credential is still going through certification
review.

My current FlyRank status shows that I have completed the required
assignment threshold and that my capstone is under review. The
certificate and credential ID will become available after the
certification process is finalized.

I will add the graduate badge to the portfolio footer once the
credential is issued, as permitted by the assignment guidance.

## Evidence

The following screenshots are included with this submission:

1.  `goat-counter.png` --- confirms that GoatCounter registered a
    portfolio visit.
2.  `OG-image.png` --- shows the portfolio image used for the social
    share preview.
3.  `Open Graph Verification.png` --- confirms the Open Graph metadata
    is working correctly.

## Final status

The portfolio is live, analytics are working, and the social sharing
metadata has been verified. The graduate badge is the only remaining
item and will be added after FlyRank issues the credential.
